{
  "name": "libavif-with-gainmap-batch-tool",
  "version": "1.0.0",
  "private": true,
  "description": "Batch convert JPEG gain map images to AVIF gain map images.",
  "main": "src/convert.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "convert": "node src/convert.js",
    "start": "npm run convert"
  },
  "dependencies": {
    "libavif-with-gainmap": "^0.1.12"
  }
}
